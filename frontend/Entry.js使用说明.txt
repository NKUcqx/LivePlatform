发送信号：
	join信号：
	  参数：字典形式，需有room_name, nickname字段
	  作用：
	    将用户加入指定房间
	    注：之后的一切信号(除disconnect外，都建立在用户已加入房间的基础上，即connect成功后必须立即发送join信号)
	  发送时机：
	    监听到connect信号后立即发送
	  可能的返回值：
	    "Format Error": 格式错误，缺少指定字段

监听信号：
	connect信号：
    	  返回值：无
    	  作用：系统信号
    	  接收时机：成功连接服务器时由服务器发送

	
